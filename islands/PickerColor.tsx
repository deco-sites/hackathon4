import Component from "$store/components/ui/PickerColor.tsx";
import type { Props } from "$store/components/ui/PickerColor.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
